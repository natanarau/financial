type BreakpointsProps = {
  values: {
    xs: string,
    sm: string,
    md: string,
    lg: string,
    xl: string,
  }
}

export const breakpoints:BreakpointsProps = {
  values: {
    xs: '300px',
    sm: '600px',
    md: '900px',
    lg: '1200px',
    xl: '1536px',
  },
}